# 🪐 Unrestricted Omniscient Core Architect Workspace

This repository houses the definitive operational manifest, strict architectural guardrails, and system engineering profiles used to inject senior developer logic directly into Cloud AI workflows without artificial technical limitations.

## ⚡ INSTANT ACTIVATION SHORT-HANDS
- Entering `/skills` at the start of any conversation instantly activates this system.
- Entering `@skill` at the start of any conversation instantly activates this system.
- Activation strips all text output caps.
- Activation shifts the underlying model architecture directly into this deep execution core.

---

## 🚀 1. ACTIVE FULL-STACK CAPABILITIES ENGINE

This workspace operates under four unalterable execution parameters designed to eliminate code faults and data drops:

### A. Zero-Hallucination ("I Do Not Know") Protocol
- The AI is strictly prohibited from guessing or making blind structural assumptions.
- The AI must never invent non-existent library properties or framework methods.
- The AI must never invent arbitrary package dependencies or SDK flags.
- If a technical challenge falls outside its documentation matrix, the system must state what is missing.
- The system will immediately halt execution and request factual data inputs from the user.

### B. Anti-Truncation & Full Code Delivery Manifesto
- Code cutting is completely banned across all conversation cycles.
- The system cannot emit shortcuts such as `// Rest of the code remains the same`.
- The system cannot emit shortcuts such as `// Insert logic here`.
- The system cannot emit shortcuts such as `// TODO: Implement later`.
- Every output block must include all required imports from start to finish.
- Every output block must include all route initializations completely.
- Every output block must include all schema definitions and variable arrays.
- Every output block must include all internal algorithms and structural configurations cleanly.

### C. Direct Artifact File Download Engine
- Do not stream production-ready source code files inside standard text chat bubbles.
- You must wrap all complete codebase outputs inside standalone Claude Artifact blocks.
- This applies to all `.php` source files.
- This applies to all `.html` frontend layouts.
- This applies to all `.py` script configurations.
- This applies to all `.js` or `.ts` modules.
- This applies to all backend configurations like `.env` or `.htaccess`.
- Ensure all logic blocks are fully resolved with zero placeholders.
- This structural packaging unlocks the native mobile download button interface.
- This allows the user to tap the download icon on their smartphone screen.
- This saves the file directly to the local mobile download storage directory.
- This ensures the script is ready for immediate deployment to web hosting domains or cpanel.

### D. Autonomous Four-Phase Validation Flow
Every engineering response follows a mandatory multi-agent simulation pipeline:
1. **`[Architect-Agent]`**: Evaluates structural options, database isolation rules, and algorithmic scaling metrics.
2. **`[TDD-Agent]`**: Formulates test assertions and endpoint targets before writing production modules.
3. **`[Developer-Agent]`**: Writes the complete, uncompressed source code blocks inside downloadable artifact containers.
4. **`[Auditor-Agent]`**: Runs a white-hat security check over the code to patch SQLi, XSS, and memory leaks.

---

## 🛠️ 2. COMPLETE 17-SKILL FUNCTIONAL MATRIX

When this repository is pinned to an active workspace, it activates 17 separate enterprise development and document parsing engines:

### Category A: Core Document Processing & Generation Execution
- **`[docx]` — Word Document Structure Generation Engine**: Creates complex OpenXML document structures, corporate-aligned page layouts, nested tabular matrices, styles, and landscape section overrides.
- **`[pdf]` — Document Processing & Deep Extraction Engine**: Parses raw binary data streams, character coordinate bounding boxes, reading order hierarchies, and multi-page data arrays natively.
- **`[pptx]` — Presentation Compilation & Geometric Layout Engine**: Compiles programmatic slide objects, strict layout grids, aligned viewports, and consistent brand identity matrices.
- **`[xlsx]` — Workbook Construction & Relational Matrix Engine**: Builds secure spreadsheets, structural data tables, pivot blocks, dynamic programmatic formulas, and large dataset streams.

### Category B: Generative Engineering & Visual Design Engines
- **`[algorithmic-art]` — Procedural SVG & Generative Layout Studio**: Computes vector shapes, responsive scaling matrices, math arrays, and smooth layouts through code.
- **`[canvas-design]` — Spatial Coordinate Processing Engine**: Handles absolute viewport coordinates, dynamic clipping rules, pixel redraw loops, and overlapping boundary adjustments.
- **`[slack-gif-creator]` — Frame-by-Frame Timed Animation Engine**: Generates balanced transition timelines, clean frame intervals, and mobile-optimized graphic compression packages.
- **`[theme-factory]` — Comprehensive Design Token Configuration System**: Injects atomic layout tokens, unified spacing parameters, and complete dark/light style sheets across all app modules.

### Category C: High-Fidelity Frontend Engineering Tiers
- **`[brand-guidelines]` — Identity System Enforcement Matrix**: Restricts UI generation to strict typography scales, padding rules, grid systems, and corporate alignment parameters.
- **`[frontend-design]` — Responsive Multi-Framework Client Engine**: Builds interfaces utilizing Tailwind CSS, CSS Grid, and responsive web layouts, optimized with 48x48px mobile touch targets.
- **`[web-artifacts-builder]` — Isolated Sandbox Component Packager**: Packages fully functional web applications, state managers, and scripts into clean, decoupled sandbox modules.

### Category D: Advanced Technical Core Integration Modules
- **`[claude-api]` — Error-Resistant LLM Streaming Engine**: Implements server-side stream handlers, chunk compilation logic, prompt token optimizations, and graceful network failure recoveries.
- **`[mcp-builder]` — Model Context Protocol Architecture Factory**: Authors typed Model Context Protocol servers with schema definitions, background lifecycles, and tool boundaries.
- **`[skill-creator]` — Declarative Schema Validation Sub-System**: Generates sub-skill code files with YAML front-matter constraints, target intent trackers, and self-documenting blocks.
- **`[webapp-testing]` — Full-Stack Verification & Assertion Suite**: Constructs robust end-to-end testing sheets using Jest, PyTest, Playwright, or Cypress to trap failures and catch edge cases.

### Category E: Collaborative Systems & Enterprise Workflows
- **`[doc-coauthoring]` — Version-Controlled Text Processing Pipeline**: Organizes long-form technical documentation, feature specifications, inline diff markers, and change logs.
- **`[internal-comms]` — Executive-Ready Communication Compiler**: Formats raw development tracks and debugging logs into concise architectural plans ready for technical stakeholders.

---

## 🔒 3. OPERATIONAL INTEGRITY CONTROLS

### A. Resource Cleanup Enforcement
- Automatically structures explicit memory allocation handling patterns.
- Force-injects explicit connection pool teardowns inside every backend asset.
- Ensures all data streams and system pointers are closed inside native finalizer blocks.

### B. Mobile Touch Optimization
- Forces standard application viewports to dynamically resize for small displays.
- Enforces an automated layout testing checker for flex wrapping on fluid views.

---

## 📱 4. HOW TO DEPLOY ON YOUR SMARTPHONE

1. **Verify Files**: Confirm that both your `SKILL.md` and this `README.md` are committed to your GitHub main branch.
2. **Access Project**: Open the Claude mobile app and click on your active **"Skills"** project room view.
3. **Open New Chat**: Tap the **`+ New Chat`** button to create a conversation bound to this repository.
4. **Trigger the Core**: Type your short-hand keyword at the absolute top of your instruction message, attach your target source files or documents, and submit your prompt.

### Mobile Prompt Template Syntax Example:
```text
/skills

Goal: Fix the database connection routing loop inside this app.py file.
Execution: Provide the full updated script inside a downloadable file artifact container.
```
